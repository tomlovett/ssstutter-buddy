# frozen_string_literal: true

require 'rails_helper'

RSpec.describe 'R::ParticipantsController' do
  # let(:researcher) { create(:researcher) }
  # let(:user) { researcher.user }
  # let(:participant) { create(:participant) }

  # before { sign_in(user) }

  # describe 'GET /r/participants/:id' do
  #   it 'returns a successful response' do
  #     get "/r/participants/#{participant.id}"

  #     expect(response).to have_http_status(:success)
  #   end

  #   it 'renders the participant show page' do
  #     get "/r/participants/#{participant.id}"

  #     expect(response.body).to include('r/Participants/show')
  #   end
  # end
end
