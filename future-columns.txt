User
    generation_ip
    blacklisted
	√ remove email_address
	√ full add index to email
    √ digest_opt_out

Study
    √ autosend_url
    √ autosend_message
    √ paused_at
    √ published_at == datetime
    √ closed_at

Location
    extract from Participant and Study
    apply to User

Participant
    email_digest_opt_out
    X etiology
    X handednes

Connection
    x participant_rating
    x invitation_response -> interested
    started_at
    completed_at
    decline_feedback: string
        dropdown, with other; other starts with OTHER

ParticipantFeedback
    connection 
        OR
    researcher
    participant
    study
    concerns
    inperson_verification
    completed_digitally

ParticipantMetadata
    etiology: text
    speech_therapy_history: text
    handedness: string
    pref_age_display: string (enum)
    pref_name_reveal